# IPv4 流媒体广播项目 —— 文件结构与关系说明

## 一、项目总览

这是一个基于 **IPv4 多播（Multicast）** 的流媒体广播系统，采用经典的 **C/S 架构**：

```
┌─────────────────────────────────────────────────────────────────┐
│                         Server（服务器）                                                       │
│                                                                                                       │
│  扫描媒体目录 ──▶ 读取 MP3 ──▶ 令牌桶限速 ──▶ UDP 多播发送       │
│                                                                                                       │
│  • thr_list 线程：每秒广播一次节目单（有哪些频道）                                  │
│  • thr_channel 线程（每频道一个）：持续发送该频道的 MP3 音频数据            │
│  • mytbf 令牌桶：控制发送速率，防止网络拥塞                                          │
└──────────────────────────────────┬──────────────────────────────┘
                                   │  UDP 多播 (224.2.2.2:1989)
                                   ▼
┌─────────────────────────────────────────────────────────────────┐
│                         Client（客户端）                                                           │
│                                                                                                          │
│  加入多播组 ──▶ 接收节目单 ──▶ 用户选频道 ──▶ 接收音频 ──▶ 播放  │
│                                                                                                          │
│  fork() 出父子进程：                                                                              │
│  • 父进程：从网络收音频数据，通过管道传给子进程                                       │
│  • 子进程：exec() 调用 mpg123 播放器，从管道读取数据解码播放                    │
└─────────────────────────────────────────────────────────────────┘
```

**核心数据流：** MP3 文件 → medialib 读取 → mytbf 限速 → thr_channel 线程 → UDP 多播 → 网络 → Client 接收 → 管道 → mpg123 播放

---

## 二、目录结构

```
IPV4-linux-/
├── Makefile                  # 顶层 Makefile
├── README
├── INSTALL
├── LISENCE
└── src/
    ├── include/              # 公共头文件（协议定义、类型定义）
    │   ├── proto.h           # 网络协议定义（数据包格式、常量）
    │   └── site_type.h       # 基础类型定义（频道ID类型）
    ├── server/               # 服务器端代码
    │   ├── Makefile
    │   ├── server.c          # 服务器主程序入口
    │   ├── server_conf.h     # 服务器配置（默认参数、配置结构体）
    │   ├── medialib.h        # 媒体库模块头文件
    │   ├── medialib.c        # 媒体库模块实现（扫描目录、读取MP3）
    │   ├── mytbf.h           # 令牌桶模块头文件
    │   ├── mytbf.c           # 令牌桶模块实现（流量控制）
    │   ├── thr_list.h        # 节目单广播线程头文件
    │   ├── thr_list.c        # 节目单广播线程实现
    │   ├── thr_channel.h     # 频道数据发送线程头文件
    │   └── thr_channel.c     # 频道数据发送线程实现
    └── client/               # 客户端代码
        ├── Makefile
        ├── client.h          # 客户端配置头文件
        └── client.c          # 客户端主程序实现
```

---

## 三、逐文件详解

### 1. `src/include/site_type.h` —— 基础类型定义

```c
typedef uint8_t chnid_t;  // 频道ID的类型（8位无符号整数，范围 0~255）
```

**作用：** 项目中所有用到"频道号"的地方统一使用 `chnid_t`，而不是直接写 `uint8_t`。这样做的好处是：如果将来频道数超过 255，只需改这一个 typedef 即可。

**被谁依赖：** 被 `proto.h` 引入，间接被所有文件使用。

---

### 2. `src/include/proto.h` —— 网络协议定义（核心！）

这是整个项目**最重要的头文件**，定义了 Server 和 Client 之间通信的"语言"。

#### 常量定义

| 常量                | 值             | 含义                  |
| ----------------- | ------------- | ------------------- |
| `DEFAULT_MGROUP`  | `"224.2.2.2"` | 多播组地址（D类IP，用于UDP多播） |
| `DEFAULT_RCVPORT` | `"1989"`      | 接收端口                |
| `CHNNR`           | `100`         | 最大频道数量              |
| `LISTCHNID`       | `0`           | 节目单专用频道号（0号频道=节目单）  |
| `MINCHNID`        | `1`           | 最小的实际频道号            |
| `MAXCHNID`        | `100`         | 最大的实际频道号            |

#### 三个核心数据结构

**（1）`struct msg_channel_st`** —— 频道音频数据包
```
+--------+----------------------------------+
| chnid  | data (柔性数组，实际的MP3数据)      |
| 1字节  | 变长                              |
+--------+----------------------------------+
```
- 每个 UDP 包一个这样的结构体
- `chnid` 标识这是哪个频道的音频
- `data[1]` 是柔性数组（C99特性），实际长度 = UDP包总长 - 1

**（2）`struct msg_listentry_st`** —— 节目单中的单个频道条目
```
+--------+--------+------------------+
| chnid  | len    | desc (频道描述)    |
| 1字节  | 2字节  | 字符串            |
+--------+--------+------------------+
```
- `len`：本条目的总长度（网络字节序）
- `desc`：频道描述文本，如 `"1. Jay Chou"`

**（3）`struct msg_list_st`** —— 整个节目单数据包
```
+--------+------------------------------------------+
| chnid  | entry (多个 msg_listentry_st 紧密排列)      |
| =0     |                                          |
+--------+------------------------------------------+
```
- `chnid` 固定为 `LISTCHNID (0)`，标识这是节目单包
- `entry[1]` 是柔性数组，实际包含多个 `msg_listentry_st`

**被谁依赖：** Server 和 Client 的所有源文件都通过 `#include <proto.h>` 使用这些结构体，确保双方对数据包格式的理解一致。

---

### 3. `src/server/server_conf.h` —— 服务器配置

```c
struct server_conf_st {
    char* rcvport;     // 接收端口
    char* mulgroup;    // 多播地址
    char* media_dir;   // 媒体文件目录
    char  runmode;     // 运行模式（守护进程 / 前台）
    char* infcname;    // 网络接口名（如 "ens33"）
};
```

**作用：** 定义服务器运行所需的全部配置项，默认值在这里设定，命令行参数可以覆盖。

**被谁依赖：** `server.c`、`medialib.c`、`thr_channel.c`、`thr_list.c`

---

### 4. `src/server/mytbf.h` + `src/server/mytbf.c` —— 令牌桶流量控制

**核心问题：** 如果直接读取 MP3 文件并全速发送，可能会瞬间打满网络带宽，导致丢包。需要一个"限速器"来控制发送速率。

**令牌桶算法原理：**
```
       每秒添加 cps 个令牌
              │
              ▼
      ┌───────────────┐
      │   令牌桶       │  ← 最多存 burst 个令牌（防止突发流量）
      │  (token)      │
      └───────┬───────┘
              │
              ▼
    发送数据前必须先"取令牌"
    取不到令牌就阻塞等待
```

**本实现的特殊性 —— 多线程安全：**
- `job[]` 是一个全局数组，最多存 1024 个令牌桶
- `thr_alrm` 线程每秒遍历 `job[]`，为每个桶补充令牌
- 每个令牌桶内部有自己的 `mut`（互斥锁）+ `cond`（条件变量），保证多线程安全
- `module_load()` 使用 `pthread_once` 确保后台线程只创建一次

**对外接口：**

| 函数                              | 作用                                  |
| ------------------------------- | ----------------------------------- |
| `mytbf_init(cps, burst)`        | 创建一个令牌桶，`cps`=每秒生成令牌数，`burst`=桶容量上限 |
| `mytbf_fetchtoken(ptr, size)`   | 取令牌（如果不够就阻塞等待）                      |
| `mytbf_returntocken(ptr, size)` | 归还未用完的令牌                            |
| `mytbf_destroy(ptr)`            | 销毁令牌桶                               |

**被谁调用：** 仅在 `medialib.c` 中被使用，对上层完全透明。

---

### 5. `src/server/medialib.h` + `src/server/medialib.c` —— 媒体库管理

**职责：** 管理服务器上的所有 MP3 媒体文件，是"数据源"层。

#### 工作原理

```
server_conf.media_dir = "/media"
        │
        ▼
/media/*
  ├── ch1/          ← 一个子目录 = 一个频道
  │   ├── desc.txt  ← 第一行就是频道描述（如 "1. Jay Chou"）
  │   ├── song1.mp3
  │   └── song2.mp3
  ├── ch2/
  │   ├── desc.txt  ← "2. Mayday"
  │   ├── song1.mp3
  │   └── song2.mp3
  └── ...
```

#### 关键结构体

`struct channel_context_st` —— 每个频道的完整运行时上下文：
```c
struct channel_context_st {
    chnid_t chnid;       // 频道号
    char *desc;          // 频道描述
    glob_t mp3glob;      // glob() 找到的该频道目录下所有 .mp3 文件路径
    int pos;             // 当前播放到第几首歌（mp3glob.gl_pathv 的下标）
    int fd;              // 当前打开歌曲的文件描述符
    off_t offset;        // 当前歌曲已读取到的字节偏移量
    mytbf_t *tbf;        // 该频道的令牌桶（限速用）
};
```

#### 对外接口

| 函数 | 作用 |
|------|------|
| `medialib_getchalist(&list, &size)` | 扫描 `media_dir` 下所有子目录，构建频道列表 |
| `medialib_freechalist(ptr)` | 释放频道列表内存 |
| `medialib_readchn(chnid, buf, size)` | 从指定频道读取音频数据（带令牌桶限速） |

**核心实现细节 `medialib_readchn`：**
1. 先从令牌桶取 token（如果令牌不够会阻塞等待）
2. 用 `pread()` 从当前歌曲的 `offset` 位置读数据
3. 如果当前歌曲读完（len==0）或读出错，自动切下一首（`open_next`）==
4. 如果有读多了没用的 token，归还给令牌桶

**被谁调用：** `thr_channel.c` 的发送线程通过 `medialib_readchn` 获取音频数据。`server.c` 在初始化时调用 `medialib_getchalist` 获取频道列表。

---

### 6. `src/server/thr_list.h` + `src/server/thr_list.c` —— 节目单广播线程

**职责：** 单独一个线程，每隔 1 秒向多播组广播一次完整的节目单（频道列表）。

#### 工作流程

```
        media_dir 扫描结果
              │
              ▼
    ① 计算数据包总大小
    ② malloc 分配内存
    ③ 封装 msg_list_st 数据包（填充每个频道的 chnid + desc）
              │
              ▼
    ④ while(1) 循环：
         sendto() ──▶ UDP 多播 224.2.2.2:1989
         sleep(1)       （所有加入多播组的客户端都能收到）
```

**为什么每秒发一次？** 因为 UDP 是不可靠传输，可能丢包。客户端随时加入多播组后，最多等 1 秒就能收到节目单。

**对外接口：**

| 函数 | 作用 |
|------|------|
| `thr_list_create(list, nr_ent)` | 创建节目单广播线程 |
| `thr_list_destroy()` | 取消并销毁线程 |

---

### 7. `src/server/thr_channel.h` + `src/server/thr_channel.c` —— 频道数据发送线程

**职责：** 为**每个频道**创建一个独立的发送线程，持续读取该频道的 MP3 数据并通过 UDP 多播发送出去。

#### 工作流程

```
    for 每个频道（最多 100 个）：
        创建线程 thr_channel_snder()
              │
              ▼
        ① malloc 一个 msg_channel_st 数据包
        ② sbufp->chnid = 当前频道号
              │
              ▼
        ③ while(1)：
            medialib_readchn() ← 读 MP3 数据（内部有令牌桶限速）
            sendto()           ← UDP 多播发送
            sched_yield()      ← 主动让出 CPU
```

**100 个频道 = 100 个线程同时发送**，所有频道的数据都在同一个多播地址上，客户端通过 `chnid` 字段区分频道，只接收自己选中的频道数据。

**对外接口：**

| 函数 | 作用 |
|------|------|
| `thr_channel_create(ptr)` | 为指定频道创建发送线程 |
| `thr_channel_destroy(ptr)` | 销毁指定频道的线程 |
| `thr_channel_destroyall()` | 销毁所有频道线程 |

---

### 8. `src/server/server.c` —— 服务器主程序

**职责：** 把所有模块串起来。这是服务器的"大脑"。

#### 启动流程

```
main()
  │
  ├──① 注册信号处理（SIGTERM/SIGINT/SIGQUIT → daemon_exit 安全退出）
  ├──② 打开系统日志 openlog("netradio", ...)
  ├──③ 解析命令行参数（-M 多播地址  -P 端口  -F 前台  -D 媒体目录  -I 网卡）
  ├──④ 如果是守护进程模式 → daemonize()（fork + setsid + 重定向 fd）
  ├──⑤ socket_init()：创建 UDP 套接字，设置多播发送
  ├──⑥ medialib_getchalist()：扫描媒体目录，获取频道列表
  ├──⑦ thr_list_create()：启动节目单广播线程
  ├──⑧ for 每个频道：thr_channel_create()：启动频道数据发送线程
  └──⑨ while(1) pause();  ← 主线程永久阻塞，等待信号
```

**关键变量：**
- `sever_sd`（全局）：UDP 套接字文件描述符，所有发送线程共用
- `sndaddr`（全局）：多播目标地址（IP + 端口），所有发送线程共用

---

### 9. `src/client/client.h` + `src/client/client.c` —— 客户端

**职责：** 接收服务器发送的节目单，让用户选频道，接收该频道的音频数据并播放。

#### 启动流程

```
main()
  │
  ├──① 解析命令行参数（-M 多播地址  -P 端口  -p 播放器命令）
  ├──② socket() 创建 UDP 套接字
  ├──③ setsockopt(IP_ADD_MEMBERSHIP)：加入多播组
  ├──④ bind()：绑定本地端口
  ├──⑤ pipe() + fork()：创建父子进程
  │
  ├── 父进程（收网络数据）：
  │   ├── recvfrom() 收节目单 ──▶ 打印频道列表
  │   ├── 用户输入频道号
  │   └── while(1)：
  │       recvfrom() 收音频数据
  │       if (chnid 匹配选中的频道)：
  │           写入管道 pd[1]（传给子进程）
  │
  └── 子进程（播放）：
      dup2(pd[0], 0)          ← 把管道读端重定向到标准输入
      execl("mpg123 - > /dev/null")  ← 用 mpg123 解码播放
```

#### 父子进程通信模型

```
  网络 ──▶ recvfrom() ──▶ [父进程] ──write()──▶ pd[1] ══管道══ pd[0] ──read()──▶ [子进程: mpg123]
```

选择 fork + pipe 而不是单进程，是因为 **mpg123 默认从 stdin 读数据**，用管道 + `dup2` 重定向是最自然的方式。

---

### 10. `src/server/Makefile` —— 服务器编译

将 5 个 `.c` 文件（`server.c`、`thr_channel.c`、`thr_list.c`、`medialib.c`、`mytbf.c`）编译链接成一个 `server` 可执行文件，使用 `-pthread` 链接线程库。

### 11. `src/client/Makefile` —— 客户端编译

将 `client.c` 编译链接成一个 `client` 可执行文件。

### 12. 顶层 `Makefile`

用于一次性编译 server 和 client。

---

## 四、文件依赖关系图

```
                    ┌──────────────────┐
                    │    proto.h       │  ← 网络协议定义（数据包格式）
                    │  site_type.h     │  ← chnid_t 类型
                    └────────┬─────────┘
                             │ 被所有 .c 文件引用
            ┌────────────────┼────────────────────┐
            ▼                ▼                     ▼
    ┌──────────────┐  ┌──────────────┐    ┌──────────────┐
    │  Server 侧   │  │  公共头文件   │    │  Client 侧   │
    └──────────────┘  └──────────────┘    └──────────────┘
            │                                    │
    ┌───────┴────────┐                    ┌──────┴───────┐
    │ server_conf.h  │                    │  client.h    │
    │ (服务器配置)    │                    │ (客户端配置)  │
    └───────┬────────┘                    └──────┬───────┘
            │                                    │
    ┌───────┴────────────────────┐              │
    │         server.c           │              │
    │  ┌──────────────────────┐  │              │
    │  │ ① 解析参数            │  │              │
    │  │ ② daemonize() 守护   │  │              │
    │  │ ③ socket_init()      │  │              │
    │  │ ④ medialib_getchalist│──┼──▶ medialib.c│
    │  │ ⑤ thr_list_create    │──┼──▶ thr_list.c│
    │  │ ⑥ thr_channel_create │──┼──▶ thr_channel.c──▶ medialib_readchn()──▶ mytbf.c
    │  │ ⑦ pause()            │  │                             │
    │  └──────────────────────┘  │                    ┌────────┴────────┐
    └────────────────────────────┘                    │  mytbf 令牌桶    │
                                                      │  (流量控制)      │
                                                      └─────────────────┘
            ┌──────────────────────┐
            │      client.c        │
            │  ┌─────────────────┐ │
            │  │ ① 加入多播组     │ │
            │  │ ② fork()        │ │
            │  │ 父: recvfrom()  │ │
            │  │     → pipe      │ │
            │  │ 子: pipe → mpg123│ │
            │  └─────────────────┘ │
            └──────────────────────┘
```

---

## 五、Server → Client 通信协议总结

### 两种数据包通过同一个多播地址 `224.2.2.2:1989` 发送：

| 包类型 | chnid 值 | 内容 | 发送频率 | 发送线程 |
|--------|----------|------|---------|---------|
| **节目单包** | `0` | 所有频道的 ID + 描述 | 每秒 1 次 | `thr_list` 线程 |
| **音频数据包** | `1 ~ 100` | 指定频道的 MP3 音频数据 | 持续发送 | 每频道一个 `thr_channel` 线程 |

Client 通过检查 `chnid` 字段来区分：
- `chnid == 0` → 这是节目单
- `chnid == 用户选中的频道号` → 这是我要的音频数据，写入管道给 mpg123
- `chnid == 其他` → 忽略

---

## 六、关键技术点

| 技术 | 使用位置 | 作用 |
|------|---------|------|
| **IPv4 多播 (Multicast)** | server.c / client.c | 一对多高效传输，服务器只发一份，多播路由器复制给所有组内客户端 |
| **柔性数组** | proto.h 中的 3 个结构体 | 变长数据包，一个结构体 = 一个完整的UDP包 |
| **令牌桶算法** | mytbf.c | 控制 MP3 发送速率（128kbps），防止网络拥塞 |
| **多线程 (pthread)** | thr_list.c / thr_channel.c / mytbf.c | 节目单广播 + 每个频道独立发送 + 令牌补充 |
| **互斥锁 + 条件变量** | mytbf.c | 多线程安全地取/归还令牌 |
| **fork + pipe + dup2** | client.c | 父子进程通信，将网络数据导入 mpg123 的 stdin |
| **守护进程** | server.c (daemonize) | 服务器后台运行，脱离终端 |
| **syslog 系统日志** | server.c / medialib.c / mytbf.c / thr_*.c | 守护进程没有终端，用 syslog 记录运行信息 |
| **信号处理** | server.c | 安全退出时销毁所有线程、释放所有内存 |

---

## 七、运行流程时序图

```
  时间 ──────────────────────────────────────────────────────▶

  Server:
  [启动]──[扫描/media]──[创建节目单线程]──[创建频道线程×N]──[阻塞等待信号]
              │                │                   │
              │        每秒 sendto()        持续 sendto()
              │        节目单(chid=0)       音频数据(chnid=1,2,3...)
              │                │                   │
              ▼                ▼                   ▼
  ═══════════════════ UDP 多播 224.2.2.2:1989 ═══════════════════
              │                │                   │
              ▼                ▼                   ▼
  Client:
  [启动]──────[收节目单]──────[选频道]──────[循环收音频──▶管道──▶mpg123播放]
                │
           "1. Jay Chou"
           "2. Mayday"
           "3. Eason Chan"
           请选择: 2
                │
                ▼
           只收 chnid=2 的数据
```

---

## 八、常见疑问解答

**Q1: 为什么服务器不需要 bind()，而客户端需要？**
- 服务器是**主动发送方**，`sendto()` 会自动分配源端口，不需要 bind。它只需要通过 `setsockopt(IP_MULTICAST_IF)` 指定从哪个网卡发出。
- 客户端是**被动接收方**，必须 bind 到指定端口，才能收到发往该端口的数据。

**Q2: 为什么节目单要每秒发一次？**
- UDP 不可靠，可能丢包。客户端随时加入多播组，最多等 1 秒就能收到节目单。这是一种"周期性广播"的可靠性补偿机制。

**Q3: 100 个频道同时发送，客户端不会被其他频道的数据淹没吗？**
- 不会。客户端 `recvfrom()` 收到每个包后立即检查 `chnid`，不是自己频道的直接丢弃（`continue`），只把自己频道的写入管道。内核层面的 UDP 接收缓冲区足够处理。

**Q4: 柔性数组 `data[1]` 为什么写 1 而不是 0？**
- C99 之前不支持 `data[]`（柔性数组成员）写法，`data[1]` 是一种历史兼容写法，效果等价。编译时需要 `__attribute__((packed))` 防止编译器对齐填充。
